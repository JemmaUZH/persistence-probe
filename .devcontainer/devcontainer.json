{
  "name": "persistence-probe-minerl",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
      },
      "extensions": [
        "ms-python.python"
      ]
    }
  },
  "postCreateCommand": "python -m pip install --upgrade 'pip<24' 'setuptools<60' wheel && python -m pip install -e .",
  "remoteUser": "vscode"
}
